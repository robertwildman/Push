<h1>Listing publishes</h1>

<table>
  <thead>
    <tr>
      <th>User Id</th>
      <th>Title</th>
      <th>Message</th>
      <th>Url</th>
      <th>Topic Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @publishes.each do |publish| %>
      <tr>
        <td><%= publish.user_id %></td>
        <td><%= publish.Title %></td>
        <td><%= publish.message %></td>
        <td><%= publish.url %></td>
        <td><%= publish.topicname %></td>
        <td><%= link_to 'Show', publish %></td>
        <td><%= link_to 'Edit', edit_publish_path(publish) %></td>
        <td><%= link_to 'Destroy', publish, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Publish', new_publish_path %>
